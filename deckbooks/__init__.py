"""Cartarch Deckbooks — local prototype.

A premium digital collector's catalog for a single Commander deck. Runs locally,
reuses Cartarch's existing card-image mirror + `scryfall_cards` metadata cache,
and stores its curation state as human-readable JSON. It does NOT touch the
production database or the running app.

See README.md for the reuse boundary and how to run it.
"""
