ruff==0.15.15
# pytest is the test runner / CI gate (the test-suite pytest migration).
# Dev/CI only — NOT in requirements.txt, so the prod image is unaffected.
pytest==9.0.3
# Required by fastapi.testclient.TestClient (route->template render tests +
# the conftest `client` fixture). Dev/CI only.
httpx==0.28.1