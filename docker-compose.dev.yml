services:
  cartarch:
    build: .
    container_name: cartarch
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - APP_VERSION=dev-version
    restart: unless-stopped
